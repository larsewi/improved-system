syntax = "proto3";

package delta;

import "entry.proto";

message Delta {
  string name = 1;
  repeated entry.Entry inserts = 2;
  repeated entry.Entry deletes = 3;
  repeated entry.Entry updates = 4;
}
